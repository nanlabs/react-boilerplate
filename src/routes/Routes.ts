import { generatePath } from 'react-router';

export const Routes = {
  ROOT: '/',
  LINKS_EXAMPLES: '/links-examples',
  USER_DETAIL: '/users/:userId',
  POWERED: '/powered-by',
};

export { generatePath };
